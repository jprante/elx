apply plugin: "com.github.spotbugs"

spotbugs {
    effort = "min"
    reportLevel = "low"
    ignoreFailures = true
}

spotbugsMain {
    reports {
        xml.getRequired().set(false)
        html.getRequired().set(true)
    }
}
